import { ScreeningType } from "@/types/manager/Theater";
export const ScreeningList = ({
  screeningList,
}: {
  screeningList: ScreeningType[];
}) => {
  return (
    <div>
      <div>a</div>
    </div>
  );
};
